---
// import { Picture } from '@astrojs/image/components';

interface Props {
  imageSrc: string;
}

const { imageSrc } = Astro.props;
---

<img class="absolute h-full w-full blur-lg object-cover" src={imageSrc} alt="code background image" />

---
const { description } = Astro.props;
---
<div class="flex flex-col text-white" id="About">
	<h2 class="text-secondary-500 text-3xl mb-10">About:</h2>
	{description.split('\n').map((line) => (
		<p class="text-lg mb-2">
			{line}
		</p>
	))}
</div>

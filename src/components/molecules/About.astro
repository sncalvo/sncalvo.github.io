---
const { description } = Astro.props;
---
<div class="flex flex-col text-white gap-2" id="About">
	<h2 class="text-secondary-500 text-3xl mb-2">About:</h2>
	{description.split('\n').filter((line) => line.trim()).map((line) => (
		<p class="text-lg">
			{line}
		</p>
	))}
</div>
